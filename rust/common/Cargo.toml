[package]
name = "ggshield-common"
version.workspace = true
edition.workspace = true
publish.workspace = true
description = "Shared leaf modules (error, hash, secure_file, binary_extensions) for the ggshield ai-hook port."

[dependencies]
sha2 = { workspace = true }

[target.'cfg(unix)'.dependencies]
libc = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
