### Fixed

- On Windows, `ggshield` starts on machines without the Microsoft Visual C++
  Redistributable. Since 1.54.0 it exited silently there, because the launcher
  needed `VCRUNTIME140.dll`. `install.ps1` also reports an error when the
  installed `ggshield` cannot run.
