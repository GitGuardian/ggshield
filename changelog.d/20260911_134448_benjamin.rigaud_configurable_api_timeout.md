### Added

- The GitGuardian API request timeout is now configurable with the `timeout` config key or the `GITGUARDIAN_API_TIMEOUT` environment variable (which takes precedence). The default timeout was also raised from 60 to 120 seconds, since some API responses take longer than the old default.
