### Fixed

- A scan the GitGuardian API refused now fails instead of reporting "No secrets
  have been found". Statuses other than the ones ggshield recognised, a 429 from
  a throttling proxy among them, dropped the affected files and let the command
  exit 0, so a pre-commit hook or CI gate passed without anything being scanned.
